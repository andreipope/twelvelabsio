---
title: "List Indexes"
api: "GET https://api.twelvelabs.io/v1/indexes"
---

### Path Params

<ParamField path="page" type="integer">
  A number that identifies the page to retrieve. **Default**: `1`.
</ParamField>

<ParamField path="page_limit" type="integer">
  The number of items to return on each page. **Default**: `10`. **Max**: `100`.
</ParamField>

<ParamField path="sort_by" type="string">

The field to sort on. The following options are available:

- `updated_at`: Sorts by the time, in the
  [RFC 3339](https://datatracker.ietf.org/doc/html/rfc3339) format, when the
  index was updated.
- `created_at`: Sorts by the time, in the
  [RFC 3339](https://datatracker.ietf.org/doc/html/rfc3339) format, when the
  index was created. **Default**: `created_at`.

</ParamField>

<ParamField path="sort_option" type="string">

The sorting direction. The following options are available:

- `asc`
- `desc` **Default**: `desc`.

</ParamField>

<ParamField path="_id" type="string">
  Filter by the unique identifier of an index.
</ParamField>

<ParamField path="index_name" type="string">
  Filter by the name of the index.
</ParamField>

<ParamField path="index_options" type="string">
  Filter by the indexing options. When filtering multiple indexing options, the
  values must be comma-separated. Example: `index_options=visual,conversation`.
</ParamField>

### Response Schema

The response is in JSON format.

On success, it contains the following fields:

- `data`: An array that contains up to `page_limit` indexes.

- `page_info:` An object that provides information about pagination. It has the
  following fields:

  - `limit_per_page`: The maximum number of items on each page.
  - `page`: The page you retrieved.
  - `total_page`: The total number of pages.
  - `total_results`: The total number of results.

- `type`: Describes the type of operation (`index_list`).

On failure, it contains the following fields:

- `error_code`: Represents the code associated with the error. See the
  [Error Codes](/reference/api-reference/error-codes) page for details.

- `message`: A human-readable string describing the error.

<ResponseExample>

```json
{
  "data": [
    {
      "_id": "62a0906d47f5678267ec10a6",
      "created_at": "2022-06-08T12:05:01.126Z",
      "engine_id": "marengo",
      "index_name": "index-1",
      "index_options": ["visual", "conversation", "text_in_video"],
      "updated_at": "2022-06-09T04:50:06.274Z"
    },
    {
      "_id": "629d857c8865706ca7f890f2",
      "created_at": "2022-06-06T04:41:32.711Z",
      "engine_id": "marengo",
      "index_name": "index-2",
      "index_options": ["visual"],
      "updated_at": "2022-06-06T04:41:59.023Z"
    },
    {
      "_id": "6298d673f1090f1100476d4c",
      "created_at": "2022-06-02T15:25:39.215Z",
      "engine_id": "marengo",
      "index_name": "index-3",
      "index_options": ["visual", "conversation", "text_in_video"],
      "updated_at": "2022-06-04T04:52:57.937Z"
    }
  ],
  "page_info": {
    "limit_per_page": 10,
    "page": 1,
    "total_page": 1,
    "total_results": 3
  },
  "type": "index_list"
}
```

 </ResponseExample>
